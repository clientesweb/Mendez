"use client"

import { useState, useEffect } from "react"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { MessageCircle, Instagram, MapPin, Truck, CreditCard, Clock, Wrench, Heart } from "lucide-react"

export function MaintenancePage() {
  const [mounted, setMounted] = useState(false)

  useEffect(() => {
    setMounted(true)
  }, [])

  const whatsappNumber = "+5493546524360"
  const whatsappMessage = "Hola! Me gustaría hacer una consulta sobre sus productos."
  const whatsappUrl = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(whatsappMessage)}`
  const instagramUrl = "https://www.instagram.com/mendezmuebleshogar?igsh=aml3a3ExaTQzOG5j"

  if (!mounted) {
    return null
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-amber-50 via-white to-orange-50">
      {/* Header con logo */}
      <header className="bg-white shadow-sm border-b">
        <div className="container mx-auto px-4 py-4">
          <div className="flex items-center justify-center">
            <div className="text-center">
              <h1 className="text-2xl md:text-3xl font-bold text-amber-800 font-playfair">Mendez Muebles</h1>
              <p className="text-sm text-amber-600 mt-1">Calidad y confort para todo tu hogar</p>
            </div>
          </div>
        </div>
      </header>

      {/* Contenido principal */}
      <main className="container mx-auto px-4 py-8 md:py-16">
        <div className="max-w-4xl mx-auto">
          {/* Banner de mantenimiento */}
          <Card className="mb-8 border-amber-200 shadow-lg">
            <CardContent className="p-8 text-center">
              <div className="flex justify-center mb-6">
                <div className="bg-amber-100 p-4 rounded-full">
                  <Wrench className="h-12 w-12 text-amber-600" />
                </div>
              </div>

              <h2 className="text-3xl md:text-4xl font-bold text-gray-800 mb-4 font-playfair">
                Sitio en Mantenimiento
              </h2>

              <p className="text-lg text-gray-600 mb-6 max-w-2xl mx-auto">
                Estamos trabajando para mejorar tu experiencia de compra. Mientras tanto, puedes contactarnos
                directamente para pedidos y consultas.
              </p>

              <div className="flex items-center justify-center gap-2 text-amber-600">
                <Clock className="h-5 w-5" />
                <span className="font-medium">Volveremos pronto con novedades</span>
              </div>
            </CardContent>
          </Card>

          {/* Grid de información */}
          <div className="grid md:grid-cols-2 gap-6 mb-8">
            {/* Contacto WhatsApp */}
            <Card className="border-green-200 hover:shadow-lg transition-shadow">
              <CardContent className="p-6">
                <div className="flex items-center gap-3 mb-4">
                  <div className="bg-green-100 p-2 rounded-full">
                    <MessageCircle className="h-6 w-6 text-green-600" />
                  </div>
                  <h3 className="text-xl font-semibold text-gray-800">Pedidos y Consultas</h3>
                </div>

                <p className="text-gray-600 mb-4">
                  Contáctanos por WhatsApp para realizar pedidos o hacer consultas sobre nuestros productos.
                </p>

                <div className="space-y-3">
                  <p className="font-medium text-gray-800">+54 9 3546 52-4360</p>
                  <Button
                    onClick={() => window.open(whatsappUrl, "_blank")}
                    className="w-full bg-green-600 hover:bg-green-700 text-white"
                  >
                    <MessageCircle className="h-4 w-4 mr-2" />
                    Contactar por WhatsApp
                  </Button>
                </div>
              </CardContent>
            </Card>

            {/* Instagram */}
            <Card className="border-pink-200 hover:shadow-lg transition-shadow">
              <CardContent className="p-6">
                <div className="flex items-center gap-3 mb-4">
                  <div className="bg-pink-100 p-2 rounded-full">
                    <Instagram className="h-6 w-6 text-pink-600" />
                  </div>
                  <h3 className="text-xl font-semibold text-gray-800">Síguenos en Instagram</h3>
                </div>

                <p className="text-gray-600 mb-4">
                  No te pierdas nuestras ofertas especiales y novedades. ¡Síguenos en Instagram!
                </p>

                <Button
                  onClick={() => window.open(instagramUrl, "_blank")}
                  className="w-full bg-pink-600 hover:bg-pink-700 text-white"
                >
                  <Instagram className="h-4 w-4 mr-2" />
                  Seguir en Instagram
                </Button>
              </CardContent>
            </Card>
          </div>

          {/* Información adicional */}
          <div className="grid md:grid-cols-3 gap-6">
            {/* Ubicación y envíos */}
            <Card className="border-blue-200">
              <CardContent className="p-6 text-center">
                <div className="bg-blue-100 p-3 rounded-full w-fit mx-auto mb-4">
                  <MapPin className="h-6 w-6 text-blue-600" />
                </div>
                <h4 className="font-semibold text-gray-800 mb-2">Ubicación</h4>
                <p className="text-gray-600 text-sm mb-2">Villa del Dique, Córdoba</p>
                <div className="flex items-center justify-center gap-1 text-blue-600">
                  <Truck className="h-4 w-4" />
                  <span className="text-sm font-medium">Envíos a todo el país</span>
                </div>
              </CardContent>
            </Card>

            {/* Promociones */}
            <Card className="border-purple-200">
              <CardContent className="p-6 text-center">
                <div className="bg-purple-100 p-3 rounded-full w-fit mx-auto mb-4">
                  <CreditCard className="h-6 w-6 text-purple-600" />
                </div>
                <h4 className="font-semibold text-gray-800 mb-3">Promociones</h4>
                <div className="space-y-2">
                  <Badge variant="secondary" className="bg-purple-100 text-purple-800 text-xs">
                    Miércoles: 3 cuotas sin interés
                  </Badge>
                  <Badge variant="secondary" className="bg-purple-100 text-purple-800 text-xs">
                    Cuota Fácil: 9 y 12 cuotas sin interés
                  </Badge>
                </div>
              </CardContent>
            </Card>

            {/* Calidad */}
            <Card className="border-amber-200">
              <CardContent className="p-6 text-center">
                <div className="bg-amber-100 p-3 rounded-full w-fit mx-auto mb-4">
                  <Heart className="h-6 w-6 text-amber-600" />
                </div>
                <h4 className="font-semibold text-gray-800 mb-2">Nuestra Promesa</h4>
                <p className="text-gray-600 text-sm">Calidad y confort para todo tu hogar</p>
              </CardContent>
            </Card>
          </div>

          {/* Footer mensaje */}
          <div className="text-center mt-12 p-6 bg-white rounded-lg shadow-sm border">
            <p className="text-gray-600">
              Gracias por tu paciencia. Estamos trabajando para ofrecerte una mejor experiencia.
            </p>
            <p className="text-sm text-gray-500 mt-2">© 2024 Mendez Muebles - Todos los derechos reservados</p>
          </div>
        </div>
      </main>
    </div>
  )
}
